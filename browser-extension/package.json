{
  "name": "bilibili-oversea-browser",
  "version": "2.1.0",
  "description": "Independent privacy-first Chromium MV3 extension for local Bilibili CDN benchmarking and switching.",
  "private": true,
  "type": "module",
  "license": "MIT",
  "engines": {
    "node": ">=18"
  },
  "keywords": [
    "bilibili",
    "cdn",
    "chromium-extension",
    "manifest-v3"
  ],
  "scripts": {
    "test": "node --test",
    "check": "node --check src/engine.js && node --check src/page-bridge.js && node --check src/content.js && node --check src/worker.js && node --check src/popup.js && node --check scripts/package-extension.mjs && node --test",
    "package": "node scripts/package-extension.mjs"
  }
}
